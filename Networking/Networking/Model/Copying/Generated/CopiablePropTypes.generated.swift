// Generated using Sourcery 0.17.0 — https://github.com/krzysztofzablocki/Sourcery
// DO NOT EDIT



// MARK: - Bool

extension NullableCopiable where Wrapped == Bool? {
    public static let nullify = Self.some(nil)
}

extension NullableCopiable where Wrapped == Array<Bool>? {
    public static let nullify = Self.some(nil)
}

// MARK: - Date

extension NullableCopiable where Wrapped == Date? {
    public static let nullify = Self.some(nil)
}

extension NullableCopiable where Wrapped == Array<Date>? {
    public static let nullify = Self.some(nil)
}

// MARK: - Decimal

extension NullableCopiable where Wrapped == Decimal? {
    public static let nullify = Self.some(nil)
}

extension NullableCopiable where Wrapped == Array<Decimal>? {
    public static let nullify = Self.some(nil)
}

// MARK: - Int

extension NullableCopiable where Wrapped == Int? {
    public static let nullify = Self.some(nil)
}

extension NullableCopiable where Wrapped == Array<Int>? {
    public static let nullify = Self.some(nil)
}

// MARK: - Int16

extension NullableCopiable where Wrapped == Int16? {
    public static let nullify = Self.some(nil)
}

extension NullableCopiable where Wrapped == Array<Int16>? {
    public static let nullify = Self.some(nil)
}

// MARK: - Int32

extension NullableCopiable where Wrapped == Int32? {
    public static let nullify = Self.some(nil)
}

extension NullableCopiable where Wrapped == Array<Int32>? {
    public static let nullify = Self.some(nil)
}

// MARK: - Int64

extension NullableCopiable where Wrapped == Int64? {
    public static let nullify = Self.some(nil)
}

extension NullableCopiable where Wrapped == Array<Int64>? {
    public static let nullify = Self.some(nil)
}

// MARK: - Int8

extension NullableCopiable where Wrapped == Int8? {
    public static let nullify = Self.some(nil)
}

extension NullableCopiable where Wrapped == Array<Int8>? {
    public static let nullify = Self.some(nil)
}

// MARK: - NSDecimalNumber

extension NullableCopiable where Wrapped == NSDecimalNumber? {
    public static let nullify = Self.some(nil)
}

extension NullableCopiable where Wrapped == Array<NSDecimalNumber>? {
    public static let nullify = Self.some(nil)
}

// MARK: - String

extension NullableCopiable where Wrapped == String? {
    public static let nullify = Self.some(nil)
}

extension NullableCopiable where Wrapped == Array<String>? {
    public static let nullify = Self.some(nil)
}

// MARK: - Account

extension NullableCopiable where Wrapped == Account? {
    public static let nullify = Self.some(nil)
}

extension NullableCopiable where Wrapped == Array<Account>? {
    public static let nullify = Self.some(nil)
}

// MARK: - AccountSettings

extension NullableCopiable where Wrapped == AccountSettings? {
    public static let nullify = Self.some(nil)
}

extension NullableCopiable where Wrapped == Array<AccountSettings>? {
    public static let nullify = Self.some(nil)
}

// MARK: - Address

extension NullableCopiable where Wrapped == Address? {
    public static let nullify = Self.some(nil)
}

extension NullableCopiable where Wrapped == Array<Address>? {
    public static let nullify = Self.some(nil)
}

// MARK: - AnyCodable

extension NullableCopiable where Wrapped == AnyCodable? {
    public static let nullify = Self.some(nil)
}

extension NullableCopiable where Wrapped == Array<AnyCodable>? {
    public static let nullify = Self.some(nil)
}

// MARK: - AnyDecodable

extension NullableCopiable where Wrapped == AnyDecodable? {
    public static let nullify = Self.some(nil)
}

extension NullableCopiable where Wrapped == Array<AnyDecodable>? {
    public static let nullify = Self.some(nil)
}

// MARK: - DotcomDevice

extension NullableCopiable where Wrapped == DotcomDevice? {
    public static let nullify = Self.some(nil)
}

extension NullableCopiable where Wrapped == Array<DotcomDevice>? {
    public static let nullify = Self.some(nil)
}

// MARK: - Media

extension NullableCopiable where Wrapped == Media? {
    public static let nullify = Self.some(nil)
}

extension NullableCopiable where Wrapped == Array<Media>? {
    public static let nullify = Self.some(nil)
}

// MARK: - Note

extension NullableCopiable where Wrapped == Note? {
    public static let nullify = Self.some(nil)
}

extension NullableCopiable where Wrapped == Array<Note>? {
    public static let nullify = Self.some(nil)
}

// MARK: - NoteBlock

extension NullableCopiable where Wrapped == NoteBlock? {
    public static let nullify = Self.some(nil)
}

extension NullableCopiable where Wrapped == Array<NoteBlock>? {
    public static let nullify = Self.some(nil)
}

// MARK: - NoteHash

extension NullableCopiable where Wrapped == NoteHash? {
    public static let nullify = Self.some(nil)
}

extension NullableCopiable where Wrapped == Array<NoteHash>? {
    public static let nullify = Self.some(nil)
}

// MARK: - NoteMedia

extension NullableCopiable where Wrapped == NoteMedia? {
    public static let nullify = Self.some(nil)
}

extension NullableCopiable where Wrapped == Array<NoteMedia>? {
    public static let nullify = Self.some(nil)
}

// MARK: - NoteRange

extension NullableCopiable where Wrapped == NoteRange? {
    public static let nullify = Self.some(nil)
}

extension NullableCopiable where Wrapped == Array<NoteRange>? {
    public static let nullify = Self.some(nil)
}

// MARK: - Order

extension NullableCopiable where Wrapped == Order? {
    public static let nullify = Self.some(nil)
}

extension NullableCopiable where Wrapped == Array<Order>? {
    public static let nullify = Self.some(nil)
}

// MARK: - OrderCountItem

extension NullableCopiable where Wrapped == OrderCountItem? {
    public static let nullify = Self.some(nil)
}

extension NullableCopiable where Wrapped == Array<OrderCountItem>? {
    public static let nullify = Self.some(nil)
}

// MARK: - OrderCouponLine

extension NullableCopiable where Wrapped == OrderCouponLine? {
    public static let nullify = Self.some(nil)
}

extension NullableCopiable where Wrapped == Array<OrderCouponLine>? {
    public static let nullify = Self.some(nil)
}

// MARK: - OrderItem

extension NullableCopiable where Wrapped == OrderItem? {
    public static let nullify = Self.some(nil)
}

extension NullableCopiable where Wrapped == Array<OrderItem>? {
    public static let nullify = Self.some(nil)
}

// MARK: - OrderItemRefund

extension NullableCopiable where Wrapped == OrderItemRefund? {
    public static let nullify = Self.some(nil)
}

extension NullableCopiable where Wrapped == Array<OrderItemRefund>? {
    public static let nullify = Self.some(nil)
}

// MARK: - OrderItemTax

extension NullableCopiable where Wrapped == OrderItemTax? {
    public static let nullify = Self.some(nil)
}

extension NullableCopiable where Wrapped == Array<OrderItemTax>? {
    public static let nullify = Self.some(nil)
}

// MARK: - OrderItemTaxRefund

extension NullableCopiable where Wrapped == OrderItemTaxRefund? {
    public static let nullify = Self.some(nil)
}

extension NullableCopiable where Wrapped == Array<OrderItemTaxRefund>? {
    public static let nullify = Self.some(nil)
}

// MARK: - OrderNote

extension NullableCopiable where Wrapped == OrderNote? {
    public static let nullify = Self.some(nil)
}

extension NullableCopiable where Wrapped == Array<OrderNote>? {
    public static let nullify = Self.some(nil)
}

// MARK: - OrderRefundCondensed

extension NullableCopiable where Wrapped == OrderRefundCondensed? {
    public static let nullify = Self.some(nil)
}

extension NullableCopiable where Wrapped == Array<OrderRefundCondensed>? {
    public static let nullify = Self.some(nil)
}

// MARK: - OrderStats

extension NullableCopiable where Wrapped == OrderStats? {
    public static let nullify = Self.some(nil)
}

extension NullableCopiable where Wrapped == Array<OrderStats>? {
    public static let nullify = Self.some(nil)
}

// MARK: - OrderStatsV4

extension NullableCopiable where Wrapped == OrderStatsV4? {
    public static let nullify = Self.some(nil)
}

extension NullableCopiable where Wrapped == Array<OrderStatsV4>? {
    public static let nullify = Self.some(nil)
}

// MARK: - OrderStatsV4Interval

extension NullableCopiable where Wrapped == OrderStatsV4Interval? {
    public static let nullify = Self.some(nil)
}

extension NullableCopiable where Wrapped == Array<OrderStatsV4Interval>? {
    public static let nullify = Self.some(nil)
}

// MARK: - OrderStatsV4Totals

extension NullableCopiable where Wrapped == OrderStatsV4Totals? {
    public static let nullify = Self.some(nil)
}

extension NullableCopiable where Wrapped == Array<OrderStatsV4Totals>? {
    public static let nullify = Self.some(nil)
}

// MARK: - OrderStatus

extension NullableCopiable where Wrapped == OrderStatus? {
    public static let nullify = Self.some(nil)
}

extension NullableCopiable where Wrapped == Array<OrderStatus>? {
    public static let nullify = Self.some(nil)
}

// MARK: - Product

extension NullableCopiable where Wrapped == Product? {
    public static let nullify = Self.some(nil)
}

extension NullableCopiable where Wrapped == Array<Product>? {
    public static let nullify = Self.some(nil)
}

// MARK: - ProductAttribute

extension NullableCopiable where Wrapped == ProductAttribute? {
    public static let nullify = Self.some(nil)
}

extension NullableCopiable where Wrapped == Array<ProductAttribute>? {
    public static let nullify = Self.some(nil)
}

// MARK: - ProductCategory

extension NullableCopiable where Wrapped == ProductCategory? {
    public static let nullify = Self.some(nil)
}

extension NullableCopiable where Wrapped == Array<ProductCategory>? {
    public static let nullify = Self.some(nil)
}

// MARK: - ProductDefaultAttribute

extension NullableCopiable where Wrapped == ProductDefaultAttribute? {
    public static let nullify = Self.some(nil)
}

extension NullableCopiable where Wrapped == Array<ProductDefaultAttribute>? {
    public static let nullify = Self.some(nil)
}

// MARK: - ProductDimensions

extension NullableCopiable where Wrapped == ProductDimensions? {
    public static let nullify = Self.some(nil)
}

extension NullableCopiable where Wrapped == Array<ProductDimensions>? {
    public static let nullify = Self.some(nil)
}

// MARK: - ProductDownload

extension NullableCopiable where Wrapped == ProductDownload? {
    public static let nullify = Self.some(nil)
}

extension NullableCopiable where Wrapped == Array<ProductDownload>? {
    public static let nullify = Self.some(nil)
}

// MARK: - ProductImage

extension NullableCopiable where Wrapped == ProductImage? {
    public static let nullify = Self.some(nil)
}

extension NullableCopiable where Wrapped == Array<ProductImage>? {
    public static let nullify = Self.some(nil)
}

// MARK: - ProductReview

extension NullableCopiable where Wrapped == ProductReview? {
    public static let nullify = Self.some(nil)
}

extension NullableCopiable where Wrapped == Array<ProductReview>? {
    public static let nullify = Self.some(nil)
}

// MARK: - ProductShippingClass

extension NullableCopiable where Wrapped == ProductShippingClass? {
    public static let nullify = Self.some(nil)
}

extension NullableCopiable where Wrapped == Array<ProductShippingClass>? {
    public static let nullify = Self.some(nil)
}

// MARK: - ProductTag

extension NullableCopiable where Wrapped == ProductTag? {
    public static let nullify = Self.some(nil)
}

extension NullableCopiable where Wrapped == Array<ProductTag>? {
    public static let nullify = Self.some(nil)
}

// MARK: - ProductVariation

extension NullableCopiable where Wrapped == ProductVariation? {
    public static let nullify = Self.some(nil)
}

extension NullableCopiable where Wrapped == Array<ProductVariation>? {
    public static let nullify = Self.some(nil)
}

// MARK: - ProductVariationAttribute

extension NullableCopiable where Wrapped == ProductVariationAttribute? {
    public static let nullify = Self.some(nil)
}

extension NullableCopiable where Wrapped == Array<ProductVariationAttribute>? {
    public static let nullify = Self.some(nil)
}

// MARK: - Refund

extension NullableCopiable where Wrapped == Refund? {
    public static let nullify = Self.some(nil)
}

extension NullableCopiable where Wrapped == Array<Refund>? {
    public static let nullify = Self.some(nil)
}

// MARK: - ShipmentTracking

extension NullableCopiable where Wrapped == ShipmentTracking? {
    public static let nullify = Self.some(nil)
}

extension NullableCopiable where Wrapped == Array<ShipmentTracking>? {
    public static let nullify = Self.some(nil)
}

// MARK: - ShippingLine

extension NullableCopiable where Wrapped == ShippingLine? {
    public static let nullify = Self.some(nil)
}

extension NullableCopiable where Wrapped == Array<ShippingLine>? {
    public static let nullify = Self.some(nil)
}

// MARK: - Site

extension NullableCopiable where Wrapped == Site? {
    public static let nullify = Self.some(nil)
}

extension NullableCopiable where Wrapped == Array<Site>? {
    public static let nullify = Self.some(nil)
}

// MARK: - SiteAPI

extension NullableCopiable where Wrapped == SiteAPI? {
    public static let nullify = Self.some(nil)
}

extension NullableCopiable where Wrapped == Array<SiteAPI>? {
    public static let nullify = Self.some(nil)
}

// MARK: - SitePlan

extension NullableCopiable where Wrapped == SitePlan? {
    public static let nullify = Self.some(nil)
}

extension NullableCopiable where Wrapped == Array<SitePlan>? {
    public static let nullify = Self.some(nil)
}

// MARK: - SiteSetting

extension NullableCopiable where Wrapped == SiteSetting? {
    public static let nullify = Self.some(nil)
}

extension NullableCopiable where Wrapped == Array<SiteSetting>? {
    public static let nullify = Self.some(nil)
}

// MARK: - SiteVisitStats

extension NullableCopiable where Wrapped == SiteVisitStats? {
    public static let nullify = Self.some(nil)
}

extension NullableCopiable where Wrapped == Array<SiteVisitStats>? {
    public static let nullify = Self.some(nil)
}

// MARK: - TaxClass

extension NullableCopiable where Wrapped == TaxClass? {
    public static let nullify = Self.some(nil)
}

extension NullableCopiable where Wrapped == Array<TaxClass>? {
    public static let nullify = Self.some(nil)
}

// MARK: - TopEarnerStats

extension NullableCopiable where Wrapped == TopEarnerStats? {
    public static let nullify = Self.some(nil)
}

extension NullableCopiable where Wrapped == Array<TopEarnerStats>? {
    public static let nullify = Self.some(nil)
}

// MARK: - TopEarnerStatsItem

extension NullableCopiable where Wrapped == TopEarnerStatsItem? {
    public static let nullify = Self.some(nil)
}

extension NullableCopiable where Wrapped == Array<TopEarnerStatsItem>? {
    public static let nullify = Self.some(nil)
}
